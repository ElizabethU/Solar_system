
<style>canvas { width: 100%; height: 90% }</style>
<ul>
<% @bodies.each do |planet| %>
  <% if planet.current %>
    <li class='planet' data-name=<%= planet.name.gsub(/-/, '_').tr('(),','').to_s %> data-diameter=<%= planet.diameter %> data-color=<%= planet.color.to_s %> data-x=<%= planet.x %> data-y=<%= planet.y %> data-z=<%= planet.z %>></li>
  <% end %>
<% end %>
</ul>
<div id="info" class="modal">
  <div class='heading'></div>
  <div class="close">Close</div>
  <div class="inner-modal"></div>
</div>
<div id="instructions" class="modal">
  <p>What's happening in space is an interactive tool for learning about the solar system. On your screen are the planets and white dots representing various spacecrafts Everything is in it's real time position, updated daily. Press your arrow keys to spin the solar system around for a better 3D sense of where things are. Click on a planet or spacecraft to find out the latest discoveries.</p>
</div>